# QMReader Books Design System

## 1. 视觉主题与氛围

内容优先的编辑式阅读工作台。正文是唯一主视觉焦点，目录负责定位，AI 负责贴近语境的理解。气质克制、安静、有书卷感，但不做复古拟物。

## 2. 色板与角色

- 页面：`#f7f4ed`；面板：`#fffdf8`；正文：`#26241f`；次文：`#6b675e`
- 边界：`#e4ded2`；品牌墨青：`#1d6a5a`；柔和选中：`#e3efeb`
- 高亮：`#f5e59c`；危险：`#b4402f`
- 深色正文使用 `rgba(255,255,255,.85)`，不使用纯黑或纯白。

## 3. 排版规则

UI 使用中文系统字体栈；阅读正文默认宋体栈，可切换无衬线。UI 字重只用 400/500/600，禁斜体。阅读字号 14–22px，行高 1.5–2.1，正文行宽 560–800px。

## 4. 组件样式

工具按钮使用 Lucide 20px 图标和不小于 40px 热区。选择态使用整块柔和底色与完整轮廓，不用左侧竖线。危险操作二次确认；加载、空、错和恢复状态完整。

## 5. 布局原则

桌面端为目录 240px、弹性正文、AI 360px 的满高三栏；三栏各自滚动并隔离滚动链。移动端只保留正文主滚动，目录、设置和 AI 进入抽屉。

## 6. 深度层级

只在浮动工具条、设置弹层和移动抽屉使用轻透明材质。阴影为染底色的两层阴影；页面分组优先靠留白和细分隔，不堆卡片。

## 7. Do / Don't

Do：让上传、继续阅读、问 AI 一步可达；给图标可访问名称；保留清晰焦点。Don't：紫蓝渐变、霓虹、斜体、原生弹窗、左侧竖线、无意义常驻状态和混用图标库。

## 8. 响应式

`<768px` 单栏和抽屉；`768–1023px` 收起目录或 AI；`>=1024px` 三栏。任何宽度无横向滚动，flex 文本项 `min-width:0`。

## 9. Motion

视觉冒险度 5/10、动效强度 4/10、信息密度 6/10。按钮 120ms、浮层 180–220ms，使用强化 ease-out；不使用 `transition: all`，支持 reduced-motion。
